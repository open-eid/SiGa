# Lombok-generated members must carry JSpecify nullness annotations so they satisfy the
# @NullMarked packages in this module (e.g. a generated equals takes @Nullable Object).
lombok.addNullAnnotations = jspecify
